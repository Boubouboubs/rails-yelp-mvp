<div class="container mt-5">
  <h1>Restaurant list</h1>

  <ul>
    <% @restaurants.each do |restau| %>
      <li>
        <%= link_to restau.name, restaurant_path(restau) %> - <%= restau.category %>
      </li>
    <% end %>
  </ul>
    <%= link_to "Create new restaurant", new_restaurant_path %>
</div>
